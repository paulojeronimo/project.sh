#!/usr/bin/env bash

usage() {
  echo "Usage: ./project.sh <command> [args]"
  echo
  echo "No command modules are bundled by default in project.sh core."
  echo "Add modules under scripts/project/*.sh in your repository."
}
